# Генератор QR-кода для контактов (vCard)

Это веб-приложение на Flask позволяет быстро и легко создавать QR-коды, содержащие вашу контактную информацию в формате vCard. При наведении камеры смартфона на этот QR-код, контакт можно сразу же сохранить в адресную книгу данного смартфона.

## Возможности

-   **Генерация vCard:** Создает файл контакта с именем, должностью, телефонами, email, адресом и сайтом.
-   **Настройка полей:** Форма позволяет ввести как обязательные (ФИО, должность, email, рабочий телефон), так и при необходимости дополнительные данные (добавочный номер, мобильный телефон).
-   **Визуальный QR-код:** Генерирует чистый черно-белый QR-код с тихой зоной вокруг.
-   **Предпросмотр:** Сразу после генерации показывает полученный QR-код на странице.
-   **Скачивание и печать:** Готовое изображение можно скачать в формате PNG или отправить на печать прямо из браузера.
-   **Логирование:** Все созданные vCard записываются в лог-файл для отслеживания, модератором web-страницы.

## Технологии

-   **Backend:** Python (Flask)
-   **Генерация QR:** `qrcode` (библиотека Python)
-   **Создание vCard:** `vobject`
-   **Работа с изображениями:** `Pillow` (PIL)
-   **Frontend:** HTML, CSS, JavaScript

## Установка и запуск

Следуйте этим инструкциям, чтобы запустить проект на вашем локальном компьютере.

### 1. Клонирование репозитория

Скопируйте файлы проекта в нужную вам директорию.

### 2. Создание виртуального окружения

Откройте терминал в папке проекта и выполните:

```bash
python -m venv .venv
```

### Активируйте виртуальное окружение

- **Windows:**

```bash
.venv\Scripts\activate
```

- **macOS/Linux:**

```bash
source .venv/bin/activate
```

### 3. Установка зависимостей

Убедитесь, что виртуальное окружение активно, и установите необходимые пакеты:

```bash
pip install -r requirements.txt
```

### 4. Запуск приложения

Запустите Flask-приложение командой:

```bash
python app.py
```

### 5. Доступ к приложению

Откройте браузер и перейдите по адресу:
http://127.0.0.1:5000

## Запуск тестов

```bash
pip install -r requirements-dev.txt
python -m pytest
```

## Структура проекта

```
qr-code-generator/
│
├── app.py                     # Основной файл приложения Flask
├── static/                    # Статические файлы (CSS, JS)
│   ├── style.css
│   └── main.js
├── templates/                 # HTML-шаблоны
│   └── index.html
├── logs/                      # Папка для файлов логов
│   └── info.log               # Файл лога (создается автоматически)
└── README.md                  # Этот файл
```

## Настройка

Все настройки «под компанию» вынесены в файл `.env` — код править не нужно.

1. Создайте `.env` из примера:

```bash
cp .env.example .env
```

2. Откройте `.env` и укажите данные вашей компании:

- `COMPANY_NAME` — название организации (подставляется в должность контакта).
- `COMPANY_SITE` — адрес сайта.
- `COMPANY_ADDRESS_STREET` / `CITY` / `REGION` / `CODE` / `COUNTRY` — физический адрес.
- `FORM_EMAIL_DEFAULT` — предзаполненное значение поля email (например, домен корпоративной почты).
- `FORM_WORK_PHONE_DEFAULT` — предзаполненное значение поля рабочего телефона.
- `FLASK_DEBUG` — режим отладки (`true`/`false`, по умолчанию `true`).
- `FLASK_HOST` — адрес запуска (по умолчанию `127.0.0.1`).
- `FLASK_PORT` — порт (по умолчанию `5000`).
- `CARD_TEMPLATE_URL` — адрес для кнопки «Шаблоны визиток» (http-ссылка или сетевой путь). Кнопка показывается, только если переменная задана; по умолчанию скрыта.

Если `.env` отсутствует или переменная не задана — используются демо-значения по умолчанию. Файл `.env` не коммитится (добавлен в `.gitignore`), в репозитории лежит только пример `.env.example`.


3. Формат номера телефона

- В функции `generate_vcard()` закомментированы альтернативные способы обработки рабочего и добавочного номеров. Вы можете раскомментировать нужный блок кода, чтобы изменить то, как номер будет сохраняться в контакте (например, добавочный в поле "Заметки").

## Использование

- Заполните все поля формы. Обязательные поля отмечены звездочкой (*).
- Нажмите кнопку "Сгенерировать QR-код".
- Справа появится готовое изображение с QR-кодом.
- Нажмите "Скачать", чтобы сохранить его на компьютер, или "Печать", чтобы распечатать.

## Логирование

Все сгенерированные vCard сохраняются в файл `logs/info.log`. Это может быть полезно для отладки или аудита.

---

## Ссылка на GitHub

Исходный код данной программы доступен в открытом репозитории на GitHub:

[qr-code-generator](https://github.com/EduardParfenov/qr-code-generator/tree/master)

Вы можете клонировать репозиторий, следить за обновлениями, сообщать об ошибках или предлагать улучшения через Issues и Pull Requests.

## Лицензия (MIT)

Данный проект является открытым и свободным для использования.

Вы можете использовать, копировать, модифицировать, объединять, публиковать, распространять, сублицензировать и/или продавать копии данного программного обеспечения, а также разрешать лицам, которым предоставляется данное программное обеспечение, делать это при соблюдении следующих условий:

Данный код предоставляется "КАК ЕСТЬ", без каких-либо явных или подразумеваемых гарантий. Автор не несет ответственности за любые последствия, возникшие в результате использования данного программного обеспечения.

## Примечание

Если вы размещаете приложение на реальном сервере, укажите в файле `.env` параметры `FLASK_DEBUG=false` (в целях безопасности), `FLASK_HOST` (нужный IP-адрес) и при необходимости `FLASK_PORT`. Требуемая версия Python — 3.11 (см. `.python-version`).