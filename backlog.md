# Backlog

Идеи и планируемые улучшения. Каждый пункт перед реализацией оформляется
как change proposal через OpenSpec (`/opsx-propose`).

## Улучшения

- [ ] **Тесты** — покрыть `generate_vcard()`, `generate_qr_code()`, `create_business_card()` юнит-тестами (pytest); smoke-тест роутов Flask.
- [ ] **Валидация ввода на сервере** — сейчас только HTML5-атрибуты `required`/`type=email`; добавить проверку и человекочитаемые ошибки в `generate_card()`.
- [ ] **Валидация на клиенте** — маски ввода для телефонов, проверка email до отправки.
- [ ] **Убрать «осиротевшую» строку `Flask`** в `app.py:117`.
- [ ] **Необязательный добавочный номер** — не добавлять пустой `tel` в vCard, если поле не заполнено.
- [ ] **Выбор фона для QR** — загрузка своего шаблона-фона через форму.
- [ ] **Кнопка «Перейти в папку "Шаблоны визиток"» не работает** — ссылка захардкожена в `templates/index.html` (`localfile:server\Брендбук\Визитка`); протокол `localfile:` нестандартен и работает только в исходном окружении автора. Предложение: вынести ссылку в настройку/конфиг либо убрать кнопку из шаблона.

## Техдолг

- [ ] `debug=True` и `host` вынести в настройки для безопасного деплоя.
- [ ] Зафиксировать версию Python (`.python-version` или `runtime.txt`).
- [ ] Ротация/ограничение размера `logs/info.log`.

## Архивировано

- [x] **Вынести настройки компании из кода в конфиг** — реализовано в change `externalize-company-config` (v1.1.0): настройки компании, параметры QR и значения полей формы вынесены в `.env` + `.env.example`.
