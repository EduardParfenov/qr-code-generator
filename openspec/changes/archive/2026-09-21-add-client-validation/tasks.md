# Tasks: add-client-validation

## 1. Подготовка

- [x] 1.1 Создать change и артефакты (proposal, design, дельта спеки) — проверка: `openspec validate add-client-validation --strict` проходит

## 2. Реализация

- [x] 2.1 Маска `+7(XXX)XXX-XX-XX` в `static/main.js` для `work_phone` и `mobile`: извлечение цифр, нормализация 7/8, форматирование по мере ввода — проверка в браузере: ввод `8001234567` даёт `+7(800)123-45-67`, ввод `8...` нормализуется
- [x] 2.2 Фильтр для `ext_phone`: только цифры и дефис — проверка в браузере: буквы не вводятся
- [x] 2.3 Проверка email на submit + блок `#js-error` в `index.html`: `preventDefault()`, сообщение, фокус — проверка в браузере: кривой email не отправляется, сообщение видно без перезагрузки

## 3. Валидация и версия

- [x] 3.1 `python -m pytest` — все 17 тестов зелёные (backend не менялся) — проверка: зелёный прогон
- [x] 3.2 Обновить `VERSION` (1.3.0) и `CHANGELOG.md` — проверка: есть запись о клиентской валидации
- [x] 3.3 `openspec validate add-client-validation --strict` — проверка: валидация проходит

## 4. Ручная проверка

- [x] 4.1 Запустить приложение (`python app.py`) и проверить в браузере: маски телефонов, фильтр добавочного, блокировку кривого email, успешную генерацию с валидными данными — проверка: QR генерируется, контакт сканируется (проверено пользователем; найден и исправлен баг маски с префиксом +7)
