# Tasks: housekeeping-fixes

## 1. Подготовка

- [x] 1.1 Создать change и артефакты (proposal, design, дельты спек) — проверка: `openspec validate housekeeping-fixes --strict` проходит

## 2. Реализация

- [x] 2.1 П.5: удалить «осиротевшую» строку `Flask` перед роутом `/` — проверка: `grep -n "^Flask$" app.py` пусто, приложение работает
- [x] 2.2 П.6: в `generate_vcard()` добавлять `ext_tel`/`mobile_tel` только при непустом значении — проверка: vCard с пустыми необязательными не содержит лишних `TEL`
- [x] 2.3 П.9: `debug`/`host`/`port` из `FLASK_DEBUG`/`FLASK_HOST`/`FLASK_PORT` с прежними дефолтами — проверка: без `.env` стартует на 127.0.0.1:5000 с debug; с `.env` — по переменным
- [x] 2.4 П.11: `RotatingFileHandler` (1 МБ, 3 копии) — проверка: в `app.py` используется `RotatingFileHandler`, запись в лог работает
- [x] 2.5 П.10: создать `.python-version` (3.11.1) — проверка: `cat .python-version` → `3.11.1`
- [x] 2.6 Добавить `FLASK_*` в `.env.example` с комментариями — проверка: переменные описаны

## 3. Тесты и документация

- [x] 3.1 Обновить/добавить тесты: пустые необязательные телефоны не в vCard; заполненный мобильный — в vCard; хендлер лога — RotatingFileHandler — проверка: `python -m pytest` зелёный
- [x] 3.2 Обновить `readme.md` (FLASK_* в «Настройке», примечание про деплой через `.env`, Python 3.11) и `AGENTS.md` (убрать нюанс про строку `Flask`, обновить нюанс про debug/host) — проверка: документы соответствуют коду
- [x] 3.3 Обновить `VERSION` (2.1.0) и `CHANGELOG.md` — проверка: есть запись со всеми пятью пунктами
- [x] 3.4 `openspec validate housekeeping-fixes --strict` — проверка: валидация проходит

## 4. Ручная проверка

- [x] 4.1 Запустить приложение (`python app.py`), сгенерировать QR с пустыми необязательными полями и отсканировать — контакт без пустых телефонов; проверить запуск с `FLASK_DEBUG=false` в `.env`
