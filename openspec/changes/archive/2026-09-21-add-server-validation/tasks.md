# Tasks: add-server-validation

## 1. Подготовка

- [x] 1.1 Создать change и артефакты (proposal, design, дельта спеки) — проверка: `openspec validate add-server-validation --strict` проходит

## 2. Реализация

- [x] 2.1 Добавить функцию `validate_form(form) -> list[str]` в `app.py`: непустые `name`/`job_title`/`work_phone`, формат email — проверка: юнит-тесты функции проходят
- [x] 2.2 В `generate_card()` читать поля через `form.get()` и при ошибках рендерить `index.html` с `error` и статусом 400 — проверка: curl с пустым `name` получает 400 и текст ошибки, а не 500

## 3. Тесты и валидация

- [x] 3.1 Тесты: 400 на отсутствующее/пустое обязательное поле, 400 на кривой email, перечисление нескольких ошибок, 200 на валидные данные с пустыми необязательными — проверка: `python -m pytest` зелёный (все тесты, включая старые)
- [x] 3.2 Обновить `VERSION` (1.2.0) и `CHANGELOG.md` — проверка: есть запись о серверной валидации
- [x] 3.3 `openspec validate add-server-validation --strict` — проверка: валидация проходит

## 4. Ручная проверка

- [x] 4.1 Запустить приложение (`python app.py`), сгенерировать QR-код через форму — работает как раньше; отправить форму с кривым email (обойдя HTML5, например через curl) — видна понятная ошибка (проверено пользователем в браузере)
