# Задачи: красные звёздочки обязательных полей

## 1. Стили

- [x] 1.1 В `static/style.css` добавить в `:root` переменную `--required-color: #d32f2f` с комментарием (проверка: переменная видна в блоке `:root` рядом с остальными)
- [x] 1.2 В `static/style.css` добавить правило `label.required::after { content: '*'; color: var(--required-color); }` с поясняющим комментарием (проверка: правило присутствует в файле)

## 2. Разметка

- [x] 2.1 В `templates/index.html` убрать символ `*` из текста подписей четырёх обязательных полей (Ф.И.О., Должность, e-mail, Тел. рабочий) и добавить этим `label` класс `required` (проверка: в тексте подписей нет `*`, у четырёх label есть `class="required"`, легенда `.attention` не изменена)

## 3. Проверка

- [x] 3.1 Прогнать `python -m pytest` — все тесты проходят
- [x] 3.2 Обновить `VERSION` (патч-версия) и добавить запись в `CHANGELOG.md` (проверка: файлы содержат новую запись)
- [x] 3.3 Ручная проверка: запустить `python app.py`, открыть http://127.0.0.1:5000 — у подписей обязательных полей красные звёздочки, у необязательных звёздочек нет; сгенерировать QR через форму и убедиться, что генерация работает как раньше

## 4. Выравнивание заглушки предпросмотра

- [x] 4.1 В `templates/index.html` в ветке `{% else %}` убрать обёртку `<div class="result">` вокруг `<p class="fill-form">Здесь появится ваш QR-код</p>`, чтобы надпись стояла на одном уровне с «Заполните форму» (проверка: в ветке `{% else %}` остаётся только `<p class="fill-form">`, ветка с результатом не изменена)
- [x] 4.2 Дополнить запись `[2.2.2]` в `CHANGELOG.md` пунктом про выравнивание заглушки (проверка: пункт присутствует в разделе «Изменено» версии 2.2.2)
- [x] 4.3 Прогнать `python -m pytest` и ручную проверку: запустить `python app.py`, открыть http://127.0.0.1:5000 — надписи «Заполните форму» и «Здесь появится ваш QR-код» на одном уровне; генерация QR работает как раньше

## 5. Кнопка «Копировать»

- [x] 5.1 В `templates/index.html` добавить в `.button-group` (рядом со «Скачать» и «Печать») кнопку `<button type="button" id="copy-qr-btn" class="copy-btn">` с текстом «Копировать» и вложенным `<span class="copy-tooltip" id="copy-tooltip">QR-скопирован в буфер</span>` (проверка: после POST `/generate` кнопка и span присутствуют в HTML)
- [x] 5.2 В `static/style.css` добавить стили `.copy-btn` (как у `.print-btn`, плюс `position: relative`) и `.copy-tooltip` / `.copy-tooltip.show` (проверка: правила присутствуют, кнопка входит в общие селекторы кнопок действий)
- [x] 5.3 В `static/main.js` удалить мёртвую функцию `copyImageFallback()` и добавить в `DOMContentLoaded` обработчик клика `#copy-qr-btn`: guard на `navigator.clipboard`/`ClipboardItem`, data URL → Blob → `clipboard.write`, показ тултипа на ~2 сек (проверка: `copyImageFallback` удалена, обработчик присутствует)
- [x] 5.4 Дополнить запись `[2.2.2]` в `CHANGELOG.md` разделом «Добавлено» с пунктом про кнопку копирования (проверка: пункт присутствует)
- [x] 5.5 Прогнать `python -m pytest` и ручную проверку: запустить `python app.py`, сгенерировать QR — кнопка видна рядом со «Скачать»/«Печать», клик копирует изображение в буфер и показывает тултип; скачивание и печать работают как раньше

## 6. Доработка кнопок действий

- [x] 6.1 Переименовать кнопку «Копировать QR-code» в «Копировать QR» в `templates/index.html` (проверка: после POST `/generate` текст кнопки — «Копировать QR»)
- [x] 6.2 В `static/style.css`: надписи кнопок действий (`.download-btn`, `.print-btn`, `.copy-btn`) центрировать по обеим осям (`display: flex; align-items: center; justify-content: center`) и запретить перенос (`white-space: nowrap`); `.copy-btn` сделать зелёной (`#009639`, hover `#007e30`) (проверка: правила присутствуют, `.copy-btn` не входит в серый стиль `.print-btn`)
- [x] 6.3 Синхронизировать название кнопки и стиль в артефактах (proposal, specs, design, tasks) и `CHANGELOG.md` (проверка: «Копировать QR-code» нигде не осталось)
- [x] 6.4 Прогнать `python -m pytest` и ручную проверку: запустить `python app.py`, сгенерировать QR — все три кнопки в одинаковой высоте, надписи по центру, «Копировать QR» зелёная; копирование и тултип работают
- [x] 6.5 Переименовать кнопку «Копировать QR» в «Копировать» в `templates/index.html` и синхронизировать название в артефактах и `CHANGELOG.md` (проверка: POST `/generate` — текст кнопки «Копировать», «Копировать QR» нигде не осталось)

## 7. Высота и выравнивание кнопок

- [x] 7.1 В `static/style.css` задать единый `line-height: 1.47059` в глобальном правиле `button` и в общем правиле `.download-btn, .print-btn, .copy-btn` — эталон высоты «Сгенерировать QR-код» (проверка: правила присутствуют)
- [x] 7.2 В `static/style.css` добавить медиа-блок `@media (min-width: 969px)`: `.form-section`/`.preview-section` — flex-колонки, `#businessCardForm` — `flex: 1` + submit `margin-top: auto`, `.preview-section .result` — `flex: 1` flex-колонка, `.result img` — `margin: auto 0` (проверка: блок присутствует, зазор 20px между рядами кнопок не изменён, мобильные правила не тронуты)
- [x] 7.3 Синхронизировать артефакты (proposal, specs — новое требование, design — решение 6) и `CHANGELOG.md` (проверка: записи присутствуют, `openspec validate` проходит)
- [x] 7.4 Прогнать `python -m pytest` и ручную проверку: запустить `python app.py` с заданной `CARD_TEMPLATE_URL`, сгенерировать QR — кнопки одинаковой высоты, «Сгенерировать QR-код» и ряд «Шаблоны визиток» на одном уровне, QR по центру; повторить без `CARD_TEMPLATE_URL` — вёрстка не ломается
- [x] 7.5 Исправить смещение нижнего ряда правой колонки на 25px вверх: в медиа-блоке `@media (min-width: 969px)` обнулить `margin-bottom` у `.preview-section .result` (проверка: замер в headless Chrome — низ кнопки «Сгенерировать QR-код» и низ ряда «Шаблоны визиток» совпадают (883px = 883px), зазор между этажами кнопок 20px сохранён; без `CARD_TEMPLATE_URL` низ submit и ряда «Скачать» тоже совпадают)
