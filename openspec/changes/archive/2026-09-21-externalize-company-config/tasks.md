# Tasks: externalize-company-config

## 1. Подготовка

- [x] 1.1 Создать change и артефакты (proposal, design, дельты спек) — проверка: `openspec validate externalize-company-config --strict` проходит
- [x] 1.2 Добавить `python-dotenv` в `requirements.txt` и установить — проверка: `pip show python-dotenv` находит пакет

## 2. Реализация

- [x] 2.1 Создать `.env.example` со всеми переменными и демо-значениями — проверка: файл содержит `COMPANY_NAME`, `COMPANY_SITE`, `COMPANY_ADDRESS_*`, `QR_X_POS`, `QR_Y_POS`, `QR_SIZE_RATIO` с комментариями
- [x] 2.2 Добавить `.env` в `.gitignore` — проверка: `git status` не показывает `.env` после его создания
- [x] 2.3 В `app.py` подключить `load_dotenv()` и заменить хардкод в `generate_vcard()` на `os.getenv()` с дефолтами — проверка: в `app.py` не осталось литералов «Рога и копыта» вне значений по умолчанию
- [x] 2.4 Заменить константы `QR_X_POS`/`QR_Y_POS` и литерал `0.9` на чтение из окружения — проверка: значения читаются через `float(os.getenv(...))` с прежними дефолтами
- [x] 2.5 Вынести предзаполненные значения полей email/рабочего телефона из `index.html` в `.env` через `app.context_processor` — проверка: в шаблоне нет литералов `@roga-i-kopyta.com` и `+7(800)000-00-00`, значения подставляются из `FORM_EMAIL_DEFAULT`/`FORM_WORK_PHONE_DEFAULT`

## 3. Проверка

- [x] 3.1 Запуск без `.env` — проверка: приложение стартует, vCard содержит демо-данные (запись в `logs/info.log`)
- [x] 3.2 Запуск с `.env` (свои значения) — проверка: vCard содержит данные из `.env`, позиция/размер QR соответствуют переменным
- [x] 3.4 Форма с `.env` и без — проверка: GET `/` и POST `/generate` отдают поля email/телефона с значениями из `.env` либо с демо-дефолтами
- [x] 3.3 Обновить раздел «Настройка» в `readme.md` — проверка: readme описывает настройку через `.env`, а не правку кода

## 4. Ручная проверка

- [x] 4.1 Запустить приложение (`python app.py`), сгенерировать QR-код через форму и отсканировать камерой телефона — проверка: контакт сохраняется с данными из `.env` (проверено пользователем)
