# Tasks: add-tests

## 1. Подготовка

- [x] 1.1 Создать `requirements-dev.txt` с pytest и установить — проверка: `pip show pytest` находит пакет
- [x] 1.2 Создать пустой `conftest.py` в корне — проверка: `python -m pytest --collect-only` из корня импортирует `app` без ошибок

## 2. Тесты

- [x] 2.1 Тесты `generate_vcard`: поля FN/TITLE/EMAIL/TEL/URL/ADR, типы телефонов, валидность через `vobject.readOne` — проверка: тесты проходят
- [x] 2.2 Тест чтения настроек из окружения: `monkeypatch.setenv("COMPANY_NAME", ...)` попадает в TITLE; тест дефолтов с `monkeypatch.delenv` — проверка: проходит независимо от локального `.env`
- [x] 2.3 Тест `generate_qr_code`: возвращает квадратное PIL-изображение — проверка: тест проходит
- [x] 2.4 Тест `create_business_card`: возвращает PIL-изображение размера фона — проверка: тест проходит
- [x] 2.5 Smoke-тесты роутов через `app.test_client()`: `GET /` → 200 и форма; `POST /generate` → 200, base64-картинка и имя в ответе — проверка: тесты проходят

## 3. Документация и версия

- [x] 3.1 Обновить `readme.md` (запуск тестов) и `AGENTS.md` (раздел «Тесты: отсутствуют» устарел) — проверка: документы описывают `pip install -r requirements-dev.txt` и `python -m pytest`
- [x] 3.2 Обновить `VERSION` (1.1.1) и `CHANGELOG.md` — проверка: есть запись о добавлении тестов
- [x] 3.3 `openspec validate add-tests --strict` — проверка: валидация проходит

## 4. Ручная проверка

- [x] 4.1 Прогнать `python -m pytest` — все тесты зелёные; затем запустить приложение (`python app.py`) и сгенерировать QR через форму — приложение работает как раньше (проверено пользователем в браузере)
